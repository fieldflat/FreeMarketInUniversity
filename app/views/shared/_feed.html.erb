<% if @feed_items.any? %>
  <ol class="microposts no_list_style" style="padding: 10px;">
    <div class="row">
      <%= render @feed_items %>
    </div>
  </ol>
  <%= paginate @feed_items %>
<% end %>
