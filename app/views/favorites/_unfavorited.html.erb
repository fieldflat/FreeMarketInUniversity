<%= form_for(:favorite, url: favorite_path(Favorite.find_by(user_id: current_user.id, micropost_id: micropost.id)), html: {method: :delete}) do |f| %>
  <%= button_tag(type: "submit") do %>
    <i class="fas fa-heart favorited"></i><span><%= micropost.favorites.count %></span>
  <% end %>
<% end %>
