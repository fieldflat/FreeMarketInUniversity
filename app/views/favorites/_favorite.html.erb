<%= form_with(model: Favorite.new, remote: true, url: favorites_path(micropost_id: micropost.id), method: :post) do |f| %>
  <%= button_tag(type: "submit") do %>
    <i class="far fa-heart" id="favorite_judge_<%= micropost.id %>"></i><span id="favorite_count_<%= micropost.id %>"><%= micropost.favorites.count %></span>
  <% end %>
<% end %>
